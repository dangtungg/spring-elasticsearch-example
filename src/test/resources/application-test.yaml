spring:
  elasticsearch:
    uris: http://localhost:9200
    connection-timeout: 5s
    socket-timeout: 30s

  application:
    name: spring-elasticsearch-example-test

logging:
  level:
    org.springframework.data.elasticsearch: INFO
    org.elasticsearch: WARN
    com.example.elk: DEBUG
    org.springframework.web: INFO